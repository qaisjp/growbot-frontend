import 'typeface-roboto';

let drawerWidth = 260;

let appStyle = {
    defaultFont: {
        font: 'typeface-roboto',
        fontFamily: '"Roboto", sans-serif',
        fontWeight: 400,
        lineHeight: "1.2em"
    },
    drawerWidth
};

export default appStyle;