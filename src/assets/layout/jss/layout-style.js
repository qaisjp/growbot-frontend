let styles = theme => ({
    contextHeader: {
        marginTop: theme.spacing.unit * 6,
        padding: theme.spacing.unit * 7,
        width: '100%',
        align: 'center',
        textAlign: 'left',
        color: '#ffffff',
        background: '#339933',

    }
});

export default styles;